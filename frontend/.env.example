# Leave empty/unset to call the API on the same origin that served the page
# (recommended — the backend serves the built frontend). Set this only when the
# API runs on a different origin than the frontend.
# VITE_API_BASE_URL=http://localhost:8001
